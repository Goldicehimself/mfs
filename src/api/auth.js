import api from './axiosConfig';

export async function login(credentials) {
  // placeholder
  return { success: true };
}

export async function logout() {
  // placeholder
  return { success: true };
}